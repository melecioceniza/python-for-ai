# Finding square root of a positive number
# As we know, Python is rich in libraries. To use mathematical
# functions, we import math library. The square root of a
# positive number is obtained by using math.sqrt(x) as the
# following program illustrates:

import math

x = eval(input("Enter a positive number for finding square root : "))
print(math.sqrt(x))
