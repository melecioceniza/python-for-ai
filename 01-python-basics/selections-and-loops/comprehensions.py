# Comprehensions: concise loop+condition expressions:
#
squares = [x * x for x in range(10) if x % 2 == 0]

# Common patterns: enumerate() for indices, zip() to iterate multiple iterables, items() to iterate dict key/value